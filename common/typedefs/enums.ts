export enum ECurrency {
  AED = 'AED',
}

export enum ECasbackType {
  FIXED = 'fixed',
  PERCENTAGE = 'percentage',
}

export enum ERuleTarget {
  CATEGORY = 'category',
  BRAND = 'brand',
}

export enum ERangeCondition {
  MORE_THAN = 'more_than',
  LESS_THAN = 'less_than',
  BETWEEN = 'between',
}

export enum EAttachmentType {
  BANNER = 'banner',
  LOGO = 'logo',
  PROMOTIONAL = 'promotional',
}
